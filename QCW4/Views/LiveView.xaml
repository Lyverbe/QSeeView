<Window x:Class="QSeeView.Views.LiveView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:wf="clr-namespace:System.Windows.Forms;assembly=System.Windows.Forms"
        WindowStartupLocation="CenterOwner" WindowState="Maximized" 
        Background="{StaticResource BackgroundColor}" Foreground="{StaticResource ForegroundColor}"
        Title="Live" Height="450" Width="800" MinHeight="200" MinWidth="200">
    <ItemsControl ItemsSource="{Binding LiveMonitors}">
        <ItemsControl.ItemsPanel>
            <ItemsPanelTemplate>
                <UniformGrid Rows="{Binding ViewRowsCount}" Columns="{Binding ViewColumnsCount}" />
            </ItemsPanelTemplate>
        </ItemsControl.ItemsPanel>
        <ItemsControl.ItemTemplate>
            <DataTemplate>
                <Viewbox Margin="2">
                    <WindowsFormsHost Width="{Binding Width}" Height="{Binding Height}">
                        <wf:PictureBox x:Name="RealPlayPictureBox" />
                    </WindowsFormsHost>
                </Viewbox>
            </DataTemplate>
        </ItemsControl.ItemTemplate>
    </ItemsControl>
</Window>
