<Window x:Class="QSeeView.Views.AddAccountView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:QSeeView.Views"
        Background="{StaticResource ThemeBackgroundBrush}" Foreground="{StaticResource ThemeForegroundBrush}"
        WindowStartupLocation="CenterOwner"
        Title="Add Account" Height="250" Width="300" ResizeMode="NoResize">
    <DockPanel Margin="10">
        <UniformGrid DockPanel.Dock="Bottom" Columns="2" HorizontalAlignment="Right" Margin="0,5,0,0">
            <Button Content="OK" Command="{Binding OkCommand}" IsDefault="True" />
            <Button Content="Cancel" Command="{Binding CancelCommand}" Margin="5,0,0,0" />
        </UniformGrid>

        <StackPanel>
            <TextBlock Text="Name" />
            <TextBox Text="{Binding Name, UpdateSourceTrigger=PropertyChanged}" PreviewKeyDown="TextBox_PreviewKeyDown" MaxLength="128" />
            <TextBlock Text="Copy from" Margin="0,5,0,0" />
            <ComboBox ItemsSource="{Binding ExistingAccounts}" DisplayMemberPath="Name" SelectedItem="{Binding SelectedAccount}" />
            <TextBlock Text="Password" Margin="0,5,0,0" />
            <TextBox Text="{Binding Password, UpdateSourceTrigger=PropertyChanged}" MaxLength="128" />
        </StackPanel>
    </DockPanel>
</Window>
